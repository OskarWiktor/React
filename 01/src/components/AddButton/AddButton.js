function AddButton({ onClick }) {
  return (
    <div className="add-button--container">
      <div className="add-button" >
        <p className="add-button--plus">Add New Expense +</p>
      </div>
    </div>
  );
}
export default AddButton;
